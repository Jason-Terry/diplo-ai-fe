# Frontend env. Read at Vite build time once scaffolding lands.
# In current "plain static" mode these are reflected in js/config.js directly.

VITE_API_BASE_URL=http://localhost:8421
VITE_WS_BASE=ws://localhost:8421
